target: main
CC: g++
AR: ar
incs_root:
  - ./include
srcs_root:
  - ./source
  - ./other
main_root: ./
libs_root:
# - ./
#  - ./lib # define the root path of library(.a or .so)
libs:
#  - add
#  - ncnn # remove lib prefix and .a or .so suffix

CXXFLAGS: -std=c++11
LFLAGS: -O3 -Wall #-O3:Release -g3:Debug

